const loginController = require('./login.controller');

const userController = require('./user.controller');

const categoryController = require('./categories.controller');

const postController = require('./post.conntroller');

module.exports = {
    loginController,
    userController,
    categoryController,
    postController,
};
